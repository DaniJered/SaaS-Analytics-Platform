When the project is updated or new significant features are added:
- Check if it is necessary to update the `trickle/notes/README.md` content to reflect the new architecture, components, or database schemas.
- Ensure the feature list in the README stays up to date with the application's actual capabilities.